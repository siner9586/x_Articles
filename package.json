{
  "name": "x-articles-daily",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "collect": "tsx scripts/collect-sources.ts",
    "score": "tsx scripts/score-candidates.ts",
    "build:issue": "tsx scripts/build-issue.ts",
    "preflight": "tsx scripts/preflight.ts",
    "state:rebuild": "tsx -e \"import('./scripts/history-index.ts').then(async m => { const idx = await m.rebuildShownIndex(''); await m.writeUsedItemsCompat(idx); console.log('rebuilt shown index', idx.length); })\"",
    "indexes:rebuild": "tsx scripts/rebuild-indexes.ts",
    "qa": "tsx scripts/qa.ts",
    "test": "tsx scripts/test.ts",
    "build": "astro build",
    "daily": "tsx scripts/daily.ts"
  },
  "dependencies": {
    "astro": "^4.16.18",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "@types/node": "^20.17.9",
    "playwright": "^1.51.1",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
